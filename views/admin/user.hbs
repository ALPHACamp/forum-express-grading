<div class="row">
  <div class="col-md-12">
    <h1>{{user.name}}</h1>
  </div>
  <div class="col-md-4">
    <div class="well">
      <ul class="list-unstyled">
        <li><strong>email:</strong> {{user.email}}</li>
        <li><strong>是否為管理者:</strong> {{#if user.isAdmin}} 是 {{else}}否{{/if}}</li>
      </ul>
    </div>
  </div>
  <hr>
</div>
<a class="btn btn-outline-secondary" href="javascript:history.back()">Back</a>