<div class="w-75 mx-auto">
  <h1>新增餐廳</h1>
  <form action="/admin/restaurants" method="POST" enctype="multipart/form-data" class="mt-4">
    <div class="row gy-4">
      <div class="col-12">
        <div class="row gy-4">
          <div class="col-12 col-lg-5 d-flex flex-column">
            <label for="imageFile" class="form-label">餐廳封面</label>
            <input id="imageFile" name="image" type="file" class="form-control" value="{{restaurant.image}}" onchange="readURL(this)" targetID="previewImg">
            <div class="restaurant-img-container">
              <img id="previewImg" src="/public/images/default-restaurant.jpg" width="100%" height="290px" max-width="320px" class="mt-3" style="object-fit: cover;" />
            </div>
          </div>
          <div class="col-12 col-lg-7">
            <div class="row gy-2">
              <div class="col-12">
                <label for="name" class="form-label">餐廳名稱</label>
                <input id="name" name="name" type="text" class="form-control" />
              </div>
              <div class="col-12 col-lg-7">
                <label for="tel" class="form-label">電話</label>
                <input id="tel" name="tel" type="tel" class="form-control" oninput="telFormat(event)" maxlength="10" />
              </div>
              <div class="col-12 col-lg-5">
                <label for="openingHours" class="form-label">營業時間</label>
                <input id="openingHours" name="openingHours" type="time" class="form-control" />
              </div>
              <div class="col-12">
                <label for="address" class="form-label">地址</label>
                <input id="address" name="address" type="text" class="form-control" />
              </div>
              <div class="col-12">
                <label for="description" class="form-label">餐廳描述</label>
                <textarea id="description" name="description" class="form-control" rows="4"></textarea>
              </div>
            </div>
          </div>
        </div>
      </div>
      <hr>
    </div>
    <div class="d-flex gap-3 justify-content-end mt-4">
      <button type="button" class="btn btn-secondary" onclick="history.back()">取消</button>
      <button type="submit" class="btn btn-primary">送出</button>
    </div>
  </form>
</div>

<script>
  function telFormat(event) {
    const inputType = event.inputType
    const telInput = document.getElementById("tel")
    let tel = telInput.value
    if (inputType === "insertText") {
      telInput.value = (tel.length === 3) ? `${tel.slice(0, 2)}-${tel.slice(2)}` : tel
    } else if (inputType === "deleteContentBackward") {
      telInput.value = (tel.length === 3) ? tel.slice(0, 2) : tel
    }
  }

  function readURL(input) {
    if (input.files && input.files[0]) {
      const imageTagID = input.getAttribute("targetID");
      const reader = new FileReader()
      reader.onload = (e) => {
        const img = document.getElementById(imageTagID);
        img.setAttribute("src", e.target.result)
      }
      reader.readAsDataURL(input.files[0]);
    }
  }
</script>