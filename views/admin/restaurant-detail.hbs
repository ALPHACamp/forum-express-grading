<div class="container w-75 d-flex flex-column gap-4">
  <div class="d-flex gap-3">
    <a href="/admin/restaurants" type="button" class="btn btn-secondary">
      <i class="fa-solid fa-arrow-left"></i>
    </a>
    <a href="/admin/restaurants/{{restaurant.id}}/edit" type="button" class="btn btn-success">
      <i class="fa-solid fa-pen"></i>
    </a>
    <form action="/admin/restaurants/{{restaurant.id}}?_method=DELETE" method="POST">
      <button type="submit" class="btn btn-danger" onclick="return confirm('確定要刪除此餐廳')">
        <i class="fa-solid fa-trash-can"></i>
      </button>
    </form>
  </div>
  <div class="row gap-3 gap-md-4">
    <div class="col-12 col-md">
      <div class="restaurant-img-container">
        <img src={{restaurant.image}} alt="餐廳封面" class="image" onload="this.style.opacity=1;"
          onerror="this.src='/public/images/default-restaurant.jpg'">
      </div>
    </div>
    <div class="col-12 col-md-6 d-flex flex-column gap-2">
      <h3>{{restaurant.name}}</h3>
      <p><strong>分類：</strong>{{restaurant.Category.name}}</p>
      <p><strong>電話：</strong>{{restaurant.tel}}</p>
      <p><strong>營業時間：</strong>{{restaurant.openingHours}}</p>
      <p><strong>地址：</strong>{{restaurant.address}}</p>
    </div>
  </div>
  <div class="row">
    <hr>
    <div class="col d-flex flex-column gap-2">
      <h3>餐廳描述</h3>
      <p>{{restaurant.description}}</p>
    </div>
  </div>
</div>