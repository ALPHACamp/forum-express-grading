<div class="card mb-3">
    <div class="row g-0">
        <div class="col-md-4">
            <img src="{{user.image}}" class="img-fluid rounded-start" alt="image">
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <h5 class="card-title">{{user.name}}</h5>
                <p class="card-text">{{user.email}}</p>

                <p>{{user.Comments.length}} 已評論餐廳</p>
                
                <a class="btn btn-primary" href="/users/{{user.id}}/edit" role="button">Edit</a>

                <a class="btn btn-outline-secondary" href="/">Back</a>

                </form>
            </div>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header mb-5">
        {{user.Comments.length}} 已評論餐廳
    </div>
    <ul class="list-group list-group-flush">
        <li class="list-group-item d-flex gap-5 flex-wrap justify-content-center">
            {{#each user.Comments}}
            <a href="/restaurants/{{this.Restaurant.id}}" class="text-decoration-none">
                <img class="card-img-top img-fluid" src="{{this.Restaurant.image}}" width="286px" height="180px"
                    style="transition: opacity 0.5s; opacity:0;" onload="this.style.opacity=1;">
                <h6>{{this.Restaurant.name}}</h6>
            </a>
            {{/each}}
        </li>
    </ul>
</div>