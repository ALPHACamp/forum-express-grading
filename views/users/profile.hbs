<div class="row mt-3">
  <div class="col-md-10 m-auto">
    <div class="card">
      <div class="row">
        <div class="col-md-4">
          <img class="card-img-top" style="width: 300px;" src="{{user.image}}">
        </div>
        <div class="col-md-8">
          <h4>{{user.name}}</h4>
          <h6>{{user.email}}</h6>
          <a href="/users/{{user.id}}/edit" class="btn btn-primary">edit</a>
        </div>
      </div>
    </div>
  </div>
</div>