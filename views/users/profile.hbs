<div class="mx-auto" style="max-width: 960px;">
  {{!-- 使用者編輯區 --}}
  <div class="card mb-5 mt-4">
    <div class="row g-0">
      <div class="col-md-4">
        {{#if user.image}}
        <img src="{{user.image}}" class="img-fluid rounded-start" style="width:100%; height:100%; "
          alt="Profile_image">
        {{else}}
        <img src="https://res.cloudinary.com/dqfxgtyoi/image/upload/v1644154630/github/defaultAvatar_uapauy.png"
          class="img-fluid rounded-start" style="width:100%; height:100%; " alt="Profile_image">
        {{/if}}
      </div>
      <div class="col-md-8 ps-5 pt-2">
        <div class="card-body">
          <h2 class="card-title">{{user.name}}</h2>
          <p class="card-text mt-4">{{user.email}}</p>
          <p class="card-text"><strong>
              {{#if user.Comments.length}}
              {{user.Comments.length}}已評論餐廳
              {{else}}
              尚未評論餐廳
              {{/if}}</strong>
          </p>
          {{!-- 當點擊使用者的連結時，判斷是否為目前登入者本人，是的話可以編輯 profile 資料頁面，否則就只能瀏覽而已  --}}
          {{#ifCond user.id loginUser.id}}
          <a class="btn btn-primary" href="/users/{{user.id}}/edit">edit</a>
          {{/ifCond}}
        </div>
      </div>
    </div>
  </div>

  {{!-- 使用者評論區 --}}
  <div class="card mt-5">
    <div class="card-header">
      {{#if user.Comments.length}}
      {{user.Comments.length}}已評論餐廳
      {{else}}
      尚未評論餐廳
      {{/if}}
    </div>
    <div class="card-body" style="min-height: 100px;">
      {{#each user.Comments}}
      <a href="/restaurants/{{this.Restaurant.id}}" class="text-decoration-none">
        <img src="{{this.Restaurant.image}}" class=" center-block img-responsive img-thumbnail m-2"
          style="width: 80px; height:80px" alt="restaurant-image">
      </a>
      {{/each}}
    </div>
  </div>
</div>