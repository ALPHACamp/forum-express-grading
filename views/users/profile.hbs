<div class="row mb-5">
  <div class="col-md-4">
    <img src="{{user.image}}" alt="photo" class="w-100">
  </div>

  <div class="col-md-8">
    <h1 class="mb-3">{{user.name}}</h1>
    <div class="mb-3">{{user.email}}</div>
    <div>
      <a href="/users/{{user.id}}/edit" class="btn btn-primary">edit</a>
    </div>
  </div>

</div>