<div class="card mb-3">
    <div class="row g-0">
        <div class="col-md-4">
            <img src="{{user.avatar}}" class="img-fluid rounded-start" alt="image">
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <h5 class="card-title">{{user.name}}</h5>
                <p class="card-text">{{user.email}}</p>

                <a class="btn btn-primary" href="/users/{{user.id}}/edit" role="button">Edit</a>

                <a class="btn btn-outline-secondary" href="/">Back</a>

                </form>
            </div>
        </div>
        <div class="col-md-8 mt-3">
            <h2>評論:</h2>
            <div class="card-body d-flex" style="width:100%">
              <ul class="list-group list-group-flush d-flex flex-row justify-content-start" style="border:2px solid #e0e0e0; width:100%">
                {{#each user.Comments}}
                <li class="list-group-item " style="width:100px; height:100px">
                    <a href="/restaurants/{{this.Restaurant.id}}" class="text-decoration-none">
                        <img class="card-img-top img-fluid" src="{{this.Restaurant.image}}" width="286px" height="180px"
                            style="transition: opacity 0.5s; opacity:0;" onload="this.style.opacity=1;">
                        <h6>{{this.Restaurant.name}}</h6>
                    </a>
                </li>
                {{/each}}
              </ul>
            </div>
        </div>
    </div>
</div>