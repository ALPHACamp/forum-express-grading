<div class="card mb-3">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{user.avatar}}" class="img-fluid rounded-start" alt="{{user.name}}'s avatar">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h3 class="card-title">{{user.name}}</h5>
        <span class="card-text">{{user.email}}</span>
      </div>
      <div class="card-body comments">
        <span class="card-text">{{commentAmount}} 個已評論餐廳</span>
      </div>
      <div class="button-container m-3">
        <a href="/users/{{user.id}}/edit" class="btn btn-primary">edit</a>
      </div>
    </div>
  </div>
</div>
<div class="card mb-3">
  <div class="card-header">
    {{commentAmount}} 個已評論餐廳
  </div>
  <div class="card-body d-flex flex-wrap">
    {{#if commentAmount}}
    {{#each comments}}
    <div class="img-container row m-1" >
      <a href="/restaurants/{{this.Restaurant.id}}">
        <img src="{{this.Restaurant.image}}" class="img-fluid rounded-start" alt="{{this.Restaurant.name}}'s img" style="width: 100px; height: 100px;">
      </a>
    </div>
    {{/each}}
    {{else}}
    <span>您還沒有對任何餐廳作出評論！</span>
    {{/if}}
  </div>
</div>