<div class="card w-50 mx-auto">
  <div class="row align-items-center justify-content-between" style="height: 300px;">
    <div class="col-md-5">
      <img src="{{user.image}}" class="ms-3 rounded-circle" alt="..." style="height: 250px; width: 250px;">
    </div>
    <div class="col-md-6">
      <div class="card-body">
        <h5 class="card-title">{{user.name}}</h5>
        <p class="card-text">{{user.email}}</p>
        <a class="btn btn-primary mt-5 btn-sm" href="/users/{{user.id}}/edit">Edit</a>
      </div>
    </div>
  </div>
</div>
{{!-- comments --}}
{{!-- <div class="card mx-auto mt-3">
  <div class="card-header">
    {{length user.Comments}} 家已評論餐廳
  </div>
  <div class="card-body">
    {{#each user.Comments}}
    <a href="/restaurants/{{this.Restaurant.id}}" class="me-2" style="text-decoration:none;">
      <img class="rounded" style="height: 100px; width: 100px;" src="{{this.Restaurant.image}}"
        alt="{{this.Restaurant.name}}">
    </a>
    {{/each}}
  </div>
</div> --}}
{{!-- comments no repeat --}}
<div class="card mx-auto mt-3">
  <div class="card-header">
    {{comments.length}} 家已評論餐廳
  </div>
  <div class="card-body">
    {{#each comments}}
    <a href="/restaurants/{{this.Restaurant.id}}" class="me-2" style="text-decoration:none;">
      <img class="rounded" style="height: 100px; width: 100px;" src="{{this.Restaurant.image}}"
        alt="{{this.Restaurant.name}}">
    </a>
    {{/each}}
  </div>
</div>