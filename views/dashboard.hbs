<div class="row">
  <div class="col-md-12">
    <h1>{{restaurant.name}}</h1>
    <p>
      {{#if restaurant.Category.isDeleted}}
        [無對應類別]
      {{else}}
        [{{restaurant.Category.name}}]
      {{/if}}
    </p>
  </div>
  <hr />
  <div class="col-md-12">
    <div class="well">
      <ul>
        <li><strong>瀏覽次數 : </strong>{{restaurant.viewCounts}}</li>
        <li><strong>評論數 : </strong>{{numOfComments}}</li>
        <li><strong>收藏數 : </strong>-</li>
      </ul>
    </div>
  </div>
</div>
<a href="javascript:history.back()">回上一頁</a>